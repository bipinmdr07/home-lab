services:
  traefik:
    networks:
      default:
        aliases:
          - ${DOMAIN}
          - adguard.${DOMAIN}
          - cronmaster.${DOMAIN}
          - downly.${DOMAIN}
          - dynamodb.${DOMAIN}
          - dynamodb-dashboard.${DOMAIN}
          - immich.${DOMAIN}
          - jellyfin.${DOMAIN}
          - minio.${DOMAIN}
          - n8n.${DOMAIN}
          - nextcloud.${DOMAIN}
          - pgweb.${DOMAIN}
          - postgres.${DOMAIN}
          - redis.${DOMAIN}
          - redis-insight.${DOMAIN}
          - syncthing.${DOMAIN}
          - traefik.${DOMAIN}
