services:
  traefik:
    networks:
      default:
        aliases:
          - ${DOMAIN}
          - adguard.${DOMAIN}
          - cronmaster.${DOMAIN}
          - downly.${DOMAIN}
          - dynamodb.${DOMAIN}
          - dynamodb-dashboard.${DOMAIN}
          - traefik.${DOMAIN}
