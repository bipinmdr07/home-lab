services:
  traefik:
    networks:
      default:
        aliases:
          - ${DOMAIN}
          - adguard.${DOMAIN}
          - cronmaster.${DOMAIN}
          - downly.${DOMAIN}
          - dynamodb.${DOMAIN}
          - dynamodb-dashboard.${DOMAIN}
          - pgweb.${DOMAIN}
          - postgres.${DOMAIN}
          - redis.${DOMAIN}
          - redis-insight.${DOMAIN}
          - traefik.${DOMAIN}
