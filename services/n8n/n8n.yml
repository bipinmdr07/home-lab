services:
  n8n:
    build: .
    container_name: n8n
    restart: always
    ports:
      - 5678:5678
    environment:
      - N8N_SECURE_COOKIE=false
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_PORT=5678
      - N8N_RUNNERS_ENABLED=true
      - NODE_ENV=production
      - WEBHOOK_URL=http://n8n.${DOMAIN:-orangepi2.box}/
      - GENERIC_TIMEZONE=${TZ}
      - TZ=${TZ}
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_DATABASE=n8n
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_USER=${DB_USER}
      - DB_POSTGRESDB_PASSWORD=${DB_PASSWORD}
      - DB_POSTGRESDB_SCHEMA=public
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${DATA_PATH}/n8n/:/home/node/.n8n
      - ${DATA_PATH}/n8n_files/:/files
      - shared_data:/mnt/shared
    networks:
      - default
      - database
    labels:
      - traefik.enable=true
      - traefik.http.routers.n8n.rule=Host(`n8n.${DOMAIN:-orangepi2.box}`)
      - traefik.http.routers.n8n.entrypoints=web
      - traefik.http.services.n8n.loadbalancer.server.port=5678
      - homepage.group=Productivity
      - homepage.name=n8n
      - homepage.icon=si-n8n
      - homepage.href=http://n8n.${DOMAIN:-orangepi2.box}
